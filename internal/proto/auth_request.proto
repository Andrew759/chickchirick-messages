syntax = "proto3";

package auth;
option go_package = "internal/gen/auth";

service AuthService {
  rpc ValidateToken(ValidateRequest) returns (ValidateResponse);
}

message ValidateRequest {
  string token = 1;
}

message ValidateResponse {
  bool valid = 1;
  string user_uuid = 2;
}